import styles from '../styles/ExplorerScreen'
import { NativeBaseProvider, Box, Text, Stack, Slider, AspectRatio, Image, Center, Heading, HStack } from "native-base";
import React, { useState } from "react";

export default function ExplorerScreen() {


  return (
    <NativeBaseProvider>
      
    </NativeBaseProvider>
  );
}